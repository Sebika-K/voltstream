"""API layer: routing, request/response shapes, and status codes.

Per the TDD's layered architecture, this layer should stay thin -- business logic
belongs in a service layer, and persistence in a repository layer, both introduced in
later phases as features that need them are implemented.
"""
